# Basic echo
- name: Hello World
  uses: builtin/echo
  with:
    message: "Hello World"

# Shell command with output capture
- id: date
  name: Get date
  uses: builtin/shell
  with:
    command: date

# Use variables
- name: Print with variables
  uses: builtin/echo
  with:
    message: "Date: ${{ outputs.date }}, Home: ${{ env.HOME }}, Arg: ${{ args.0 }}"

# Output redirection
- name: Redirect to file
  uses: builtin/shell
  with:
    command: echo "logged"
    stdout: /tmp/transformer.log

- name: Discard output
  uses: builtin/shell
  with:
    command: echo "suppressed"
    stdout: discard